services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: talking_bot
      POSTGRES_PASSWORD: talking_bot
      POSTGRES_DB: talking_bot
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
